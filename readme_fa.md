# 🤖 ربات دانلودر تلگرام

<div align="center">

![Python](https://img.shields.io/badge/python-v3.11+-blue.svg)
![Aiogram](https://img.shields.io/badge/aiogram-v3.7.0-green.svg)
![License](https://img.shields.io/badge/license-MIT-blue.svg)
![Status](https://img.shields.io/badge/status-active-success.svg)

یک ربات تلگرام قدرتمند و حرفه‌ای برای دانلود فایل با امکانات پیشرفته و پنل مدیریت

[ویژگی‌ها](#ویژگی‌ها) • [نصب](#نصب) • [استفاده](#استفاده) • [پنل ادمین](#پنل-ادمین) • [مشارکت](#مشارکت)

</div>

---

## ✨ ویژگی‌ها

### 📥 قابلیت‌های دانلود
- ✅ دانلود فایل تا 2 گیگابایت (محدودیت تلگرام)
- ✅ تقسیم خودکار فایل‌های بزرگتر
- ✅ قابلیت رزومه برای دانلودهای قطع شده
- ✅ نمایش پیشرفت بلادرنگ با سرعت و زمان باقی‌مانده
- ✅ پشتیبانی از همه انواع فایل
- ✅ انتخاب فرمت ارسال (عکس، ویدیو، صدا، سند)

### 👥 مدیریت کاربران
- ✅ محدودیت دانلود روزانه
- ✅ سیستم VIP با دانلود نامحدود
- ✅ آمار و فعالیت کاربران
- ✅ قابلیت بن/آنبن
- ✅ نمودار استفاده شخصی

### 🔧 پنل ادمین
- ✅ داشبورد آمار جامع
- ✅ نمودار فعالیت 14 روزه
- ✅ رابط مدیریت کاربران
- ✅ آپلود فایل با کد یونیک
- ✅ کنترل تنظیمات سیستم
- ✅ پیام همگانی
- ✅ پاکسازی خودکار

### 🛡️ امنیت و عملکرد
- ✅ دیتابیس SQLite با تراکنش
- ✅ محافظت در برابر حملات
- ✅ مدیریت خطا و تلاش مجدد
- ✅ سیستم بکاپ خودکار
- ✅ لاگ‌گیری دقیق

## 📋 پیش‌نیازها

- Python 3.8+
- Ubuntu 20.04+ (یا هر توزیع لینوکس)
- حداقل 1GB رم
- 10GB فضای دیسک

## 🚀 نصب

### روش پیشنهادی: اسکریپت نصب خودکار (مخصوص لینوکس)

برای نصب آسان و سریع بر روی سیستم‌های لینوکسی (مانند اوبونتو)، می‌توانید از اسکریپت نصب خودکار استفاده کنید:

1.  **دانلود و اجرای اسکریپت:**
    ```bash
    git clone https://github.com/yourusername/telegram-downloader-bot.git
    cd telegram-downloader-bot
    chmod +x install.sh
    ./install.sh
    ```
    اسکریپت شما را در طی مراحل نصب راهنمایی می‌کند، از جمله دریافت توکن ربات و آیدی ادمین، و همچنین نصب به عنوان سرویس systemd و تنظیم cronjob ها برای مانیتورینگ و بکاپ (اختیاری).

### روش دستی

اگر ترجیح می‌دهید مراحل را به صورت دستی انجام دهید یا از سیستم‌عامل دیگری استفاده می‌کنید:

1.  **کلون کردن مخزن:**
    ```bash
    git clone https://github.com/yourusername/telegram-downloader-bot.git
    cd telegram-downloader-bot
    ```

2.  **ایجاد محیط مجازی:**
    ```bash
    python3 -m venv venv
    source venv/bin/activate  # در لینوکس/مک
    # venv\Scripts\activate  # در ویندوز
    ```

3.  **نصب وابستگی‌ها:**
    ```bash
    pip install --upgrade pip
    pip install -r requirements.txt
    # در لینوکس/مک، اگر python-magic-bin نصب شده باشد، بهتر است حذف شود:
    # pip uninstall python-magic-bin -y
    ```

4.  **پیکربندی ربات:**
    *   یک کپی از فایل `.env.example` با نام `.env` ایجاد کنید:
        ```bash
        cp .env.example .env
        ```
    *   فایل `.env` را باز کرده و مقادیر `BOT_TOKEN` (توکن ربات دریافتی از @BotFather) و `ADMIN_IDS` (آیدی عددی ادمین یا ادمین‌ها، جدا شده با کاما) را با مقادیر صحیح جایگزین کنید. سایر تنظیمات اختیاری نیز در این فایل قابل تغییر هستند.

5.  **ایجاد پوشه‌های مورد نیاز (اگر وجود ندارند):**
    ```bash
    mkdir -p downloads uploads temp logs backups
    ```

6.  **اجرای ربات:**
    ```bash
    python telegram_downloader_bot.py
    ```

### نصب کامل و راه‌اندازی به عنوان سرویس

برای راهنمای کامل شامل راه‌اندازی ربات به عنوان سرویس systemd (برای اجرای دائمی در پس‌زمینه)، تنظیمات مانیتورینگ و بکاپ خودکار، به فایل [INSTALL.md](INSTALL.md) (یا [INSTALL.fa.md](INSTALL.fa.md) برای فارسی) مراجعه کنید. اسکریپت نصب خودکار (`install.sh`) بسیاری از این مراحل را برای شما انجام می‌دهد.

## 🔧 پیکربندی

### متغیرهای محیطی

فایل `.env` در ریشه پروژه ایجاد کنید:

```env
# تنظیمات ربات
BOT_TOKEN=توکن_ربات_شما
ADMIN_IDS=123456789,987654321

# تنظیمات اختیاری
MAX_FILE_SIZE=2147483648  # 2GB به بایت
DEFAULT_DAILY_LIMIT=10
DOWNLOAD_TIMEOUT=3600
```

### دریافت توکن ربات

1. در تلگرام به [@BotFather](https://t.me/botfather) پیام دهید
2. دستور `/newbot` را ارسال کنید
3. نامی برای ربات انتخاب کنید
4. یوزرنیم انتخاب کنید (باید به 'bot' ختم شود)
5. توکن ارائه شده را کپی کنید

### دریافت آیدی تلگرام

1. در تلگرام به [@userinfobot](https://t.me/userinfobot) پیام دهید
2. هر پیامی ارسال کنید
3. آیدی عددی خود را کپی کنید

## 📱 استفاده

### برای کاربران

1. **شروع ربات**
   ```
   /start - راه‌اندازی ربات و مشاهده وضعیت
   ```

2. **دانلود فایل**
   - لینک مستقیم دانلود را ارسال کنید
   - فرمت ارسال را انتخاب کنید
   - فایل را دریافت کنید

3. **مشاهده آمار**
   ```
   /mystats - نمایش آمار دانلود شما
   ```

4. **دانلود با کد**
   ```
   /dl CODE - دانلود فایل با کد ارائه شده توسط ادمین
   ```

### برای ادمین‌ها

1. **دسترسی به پنل ادمین**
   ```
   /admin - باز کردن پنل کنترل ادمین
   ```

2. **امکانات ادمین**:
   - 📊 مشاهده آمار سیستم
   - 📈 تولید نمودار فعالیت
   - 👥 مدیریت کاربران (VIP، بن، محدودیت)
   - ⚙️ پیکربندی تنظیمات سیستم
   - 📤 آپلود فایل با کد یونیک
   - 🗑️ پاکسازی فایل‌های قدیمی
   - 📢 ارسال پیام همگانی

## 📊 پنل ادمین

### داشبورد آمار
- کل کاربران و دانلودها
- کاربران فعال روزانه
- میزان استفاده از فضا
- روند دانلودها

### مدیریت کاربران
- جستجو و فیلتر کاربران
- اعطا/لغو وضعیت VIP
- تنظیم محدودیت‌های سفارشی
- بن/آنبن کاربران

### تنظیمات سیستم
- فعال/غیرفعال کردن دانلود
- تنظیم محدودیت روزانه عمومی
- فعال/غیرفعال کردن حالت نگهداری
- پیکربندی پیام‌های خوش‌آمدگویی

## 🗂️ ساختار پروژه

```
telegram-downloader-bot/
├── telegram_bot.py      # کد اصلی ربات
├── requirements.txt     # وابستگی‌های پایتون
├── .env.example        # قالب متغیرهای محیطی
├── .gitignore         # فایل gitignore
├── README.md          # این فایل (انگلیسی)
├── README.fa.md       # این فایل (فارسی)
├── LICENSE            # مجوز MIT
├── INSTALL.md         # راهنمای نصب کامل
├── CONTRIBUTING.md    # راهنمای مشارکت
├── docker-compose.yml # پیکربندی Docker
├── Dockerfile         # تصویر Docker
└── scripts/           # اسکریپت‌های کمکی
    ├── backup.sh      # اسکریپت بکاپ
    ├── monitor.sh     # اسکریپت مانیتورینگ
    └── setup.sh       # اسکریپت راه‌اندازی اولیه
```

## 🤝 مشارکت

از مشارکت شما استقبال می‌کنیم! برای جزئیات [CONTRIBUTING.md](CONTRIBUTING.md) را ببینید.

## 📝 مجوز

این پروژه تحت مجوز MIT منتشر شده است - فایل [LICENSE](LICENSE) را برای جزئیات ببینید.

## 📞 پشتیبانی

- 📧 ایمیل: your.email@example.com
- 💬 تلگرام: [@yourusername](https://t.me/yourusername)
- 🐛 مشکلات: [GitHub Issues](https://github.com/yourusername/telegram-downloader-bot/issues)

---

<div align="center">
ساخته شده با ❤️ توسط <a href="https://github.com/yourusername">نام شما</a>
</div>